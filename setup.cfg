[aliases]
test=pytest

[metadata]
description-file = README.md

[tool:pytest]
addopts =
    --tb short
    -rxs
    --cov-report term-missing:skip-covered
    --cov bottle_opentracing
    tests

[coverage:run]
branch = True
